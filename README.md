# Markdown to Word Converter

## 📋 專案簡介
一個具備圖形化介面（GUI）的桌面工具，可將 Markdown (.md) 檔案精確轉換為 Microsoft Word (.docx) 檔案。

## ✨ 核心特點
1. **原生樣式映射**：將 Markdown 元素完美對應到 Word 內建標準樣式
2. **無自定義樣式**：嚴格使用 Word 內建樣式庫（Heading 1-6, Normal, List Bullet, List Number, Quote, Table Grid）
3. **完整格式支援**：
   - 標題 (H1-H6)
   - 清單（有序/無序，支援巢狀）
   - 粗體、斜體、行內程式碼
   - 表格
   - 程式碼區塊
   - 引用
   - 水平線

## 🛠️ 技術架構

### 技術選型
- **程式語言**：Python 3.8+
- **GUI 框架**：CustomTkinter（現代化的 Tkinter 介面）
- **文件處理**：python-docx（Word 文件生成）
- **路徑處理**：pathlib（跨平台路徑支援）

### 為何選擇 python-docx 而非 Pandoc？
1. **原生樣式控制**：python-docx 允許精確控制使用哪些 Word 樣式，確保只使用內建樣式
2. **無需外部依賴**：不需要安裝 Pandoc 執行檔
3. **更好的錯誤處理**：可以在 Python 層級捕捉並處理錯誤
4. **環境獨立性**：純 Python 解決方案，部署更簡單

## 📦 安裝步驟

### 1. 安裝 Python
確保已安裝 Python 3.8 或更高版本：
```bash
python --version
```

### 2. 安裝依賴套件
```bash
pip install -r requirements.txt
```

或手動安裝：
```bash
pip install customtkinter==5.2.1 python-docx==1.1.0
```

## 🚀 使用方法

### 啟動程式
```bash
python md_to_word_converter.py
```

### 操作步驟
1. 點擊「瀏覽」按鈕選擇要轉換的 Markdown 檔案
2. （選填）自訂輸出位置，預設會在相同目錄生成
3. 點擊「開始轉換」按鈕
4. 轉換完成後會顯示成功訊息，可選擇立即開啟檔案

## 🏗️ 架構說明

### 核心類別

#### `MarkdownToWordConverter`
轉換引擎核心類別，負責：
- 解析 Markdown 語法
- 建立 Word 文件
- 映射到 Word 內建樣式

#### `App`
GUI 應用程式類別，負責：
- 建立使用者介面
- 處理檔案選擇
- 顯示轉換狀態

### 樣式映射表

| Markdown 元素 | Word 內建樣式 | 說明 |
|--------------|-------------|------|
| # Heading 1 | Heading 1 | 一級標題 |
| ## Heading 2 | Heading 2 | 二級標題 |
| ### Heading 3 | Heading 3 | 三級標題 |
| #### - ###### | Heading 4-6 | 四到六級標題 |
| 一般段落 | Normal | 標準段落 |
| - 無序列表 | List Bullet | 項目符號 |
| 1. 有序列表 | List Number | 編號列表 |
| > 引用 | Quote | 引用段落 |
| 表格 | Table Grid | 表格格式 |
| **粗體** | Normal + Bold | 粗體文字 |
| *斜體* | Normal + Italic | 斜體文字 |
| `程式碼` | Normal + Courier New | 行內程式碼 |

### 如何確保使用內建樣式？

1. **明確指定樣式名稱**：
   ```python
   para = self.doc.add_paragraph(style='Normal')  # 使用內建 Normal 樣式
   heading = self.doc.add_heading(level=1)         # 使用內建 Heading 1 樣式
   ```

2. **不建立自定義樣式**：
   ```python
   # ❌ 錯誤：建立自定義樣式
   # styles = document.styles
   # style = styles.add_style('CustomStyle', WD_STYLE_TYPE.PARAGRAPH)
   
   # ✅ 正確：使用內建樣式
   para = self.doc.add_paragraph(style='Normal')
   ```

3. **格式化只透過 run 屬性**：
   ```python
   run = paragraph.add_run(text)
   run.bold = True          # 設定粗體
   run.italic = True        # 設定斜體
   run.font.name = 'Courier New'  # 設定字型（程式碼）
   ```

## 🔧 錯誤處理

程式包含完整的錯誤處理機制：

1. **檔案不存在**：檢查輸入檔案是否存在
2. **權限問題**：捕捉寫入權限錯誤
3. **編碼問題**：使用 UTF-8 編碼讀取，支援中文路徑
4. **格式錯誤**：解析失敗時提供詳細錯誤訊息

## 📝 測試範例

建立一個測試用的 Markdown 檔案：

```markdown
# 主標題

這是一個測試段落，包含**粗體**、*斜體*和`程式碼`。

## 二級標題

### 列表測試

- 項目 1
- 項目 2
  - 子項目 2.1
  - 子項目 2.2

### 有序列表

1. 第一項
2. 第二項
3. 第三項

### 表格測試

| 欄位 1 | 欄位 2 | 欄位 3 |
|--------|--------|--------|
| 資料 A | 資料 B | 資料 C |
| 資料 D | 資料 E | 資料 F |

### 程式碼區塊

\```python
def hello_world():
    print("Hello, World!")
\```

### 引用

> 這是一段引用文字。

---

結束！
```

## 🔍 進階開發

### 擴充支援的 Markdown 語法
如需支援更多 Markdown 語法，可在 `_process_line` 方法中新增解析邏輯。

### 自訂 Word 模板
若需使用特定的 Word 模板：
```python
self.doc = Document('template.docx')  # 使用現有模板
```

### 批次轉換
可擴充為支援資料夾批次轉換功能。

## ⚠️ 注意事項

1. **不支援的 Markdown 語法**：
   - 註腳
   - 任務列表 `- [ ]`
   - LaTeX 數學公式
   - 嵌入圖片（會被忽略）

2. **路徑限制**：
   - 支援含空白和中文的路徑
   - Windows 路徑長度限制（260 字元）

3. **Word 版本**：
   - 產生的 .docx 檔案相容 Word 2007 及更新版本

## 🚀 快速安裝（從 Release）

如果你不想自己配置 Python 環境，可以直接下載預編譯的執行檔：

1. 前往 [Releases 頁面](https://github.com/Wilton6328/md2docx/releases)
2. 下載最新版本對應您系統的檔案：
   - **Windows**: `md2docx-windows.exe`
   - **macOS**: `md2docx-macos`
   - **Linux**: `md2docx-linux`
3. (macOS/Linux) 賦予執行權限：
   ```bash
   chmod +x md2docx-*
   ```
4. 雙擊執行檔即可啟動程式 🎉

> **無需安裝 Python 或任何依賴套件！**

---

## 🔄 CI/CD 與自動化部署

本專案使用 GitHub Actions 實現自動化測試與發布流程。

### 📊 持續整合 (CI)

每次推送程式碼或建立 Pull Request 時，會自動執行：

- ✅ **多平台測試** - Windows, macOS, Linux
- ✅ **多版本測試** - Python 3.8, 3.9, 3.10, 3.11
- ✅ **代碼品質檢查** - Black 格式化、Flake8 語法檢查
- ✅ **單元測試** - Pytest 測試覆蓋率
- ✅ **安全性掃描** - Safety & Bandit

[![CI Status](https://github.com/Wilton6328/md2docx/workflows/CI%20-%20%E6%8C%81%E7%BA%8C%E6%95%B4%E5%90%88/badge.svg)](https://github.com/Wilton6328/md2docx/actions)

### 🎉 自動發布 (Release)

當推送版本標籤時（例如 `v1.0.0`），會自動：

1. 在多個平台構建可執行檔
2. 建立 GitHub Release
3. 上傳所有平台的執行檔

**如何建立新版本：**

```bash
# 1. 更新版本號碼（在 pyproject.toml）
# 2. 提交並打標籤
git add .
git commit -m "Release v1.0.0"
git tag v1.0.0

# 3. 推送標籤到 GitHub
git push origin v1.0.0

# GitHub Actions 會自動構建並發布！
```

### 🔐 Secrets 與環境變數

如果需要使用外部 API 或服務，可以在 GitHub Repository Settings 中設定 Secrets：

1. 前往 **Settings** → **Secrets and variables** → **Actions**
2. 點擊 **New repository secret**
3. 設定名稱（例如 `API_KEY`）與值
4. 在工作流程中使用：
   ```yaml
   env:
     API_KEY: ${{ secrets.API_KEY }}
   ```

> 本專案目前使用 `GITHUB_TOKEN`（GitHub 自動提供）與 `CODECOV_TOKEN`（選用）。

---

## 📦 專案結構

```
md2docx/
├── .github/
│   └── workflows/
│       ├── ci.yml              # 持續整合工作流程
│       └── release.yml         # 自動發布工作流程
├── __pycache__/                # Python 緩存（已忽略）
├── ARCHITECTURE.md             # 架構文件
├── QUICKSTART.md               # 快速開始指南
├── README.md                   # 本文件
├── SUMMARY.md                  # 專案摘要
├── .gitignore                  # Git 忽略規則
├── pyproject.toml              # 專案配置與依賴管理
├── requirements.txt            # 生產環境依賴
├── requirements-dev.txt        # 開發環境依賴
├── md_to_word_converter.py     # 主程式
├── test_converter.py           # 單元測試
├── test_sample.md              # 測試範例
├── test_output.docx            # 測試輸出（已忽略）
└── 啟動轉換器.bat              # Windows 快速啟動腳本
```

---

## 🛠️ 開發者指南

### 本地開發環境

```bash
# 1. Clone repository
git clone https://github.com/Wilton6328/md2docx.git
cd md2docx

# 2. 建立虛擬環境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 3. 安裝依賴
pip install -r requirements.txt -r requirements-dev.txt

# 4. 執行測試
pytest

# 5. 代碼格式化
black .

# 6. 語法檢查
flake8 .
```

### 本地構建執行檔

```bash
# 使用 PyInstaller 打包
pyinstaller --name md2docx --onefile --windowed md_to_word_converter.py

# 輸出位置: dist/md2docx.exe (Windows) 或 dist/md2docx (macOS/Linux)
```

---

## 📄 授權
MIT License

## 👤 作者
md2docx Team
