# 自動化發布工作流程
# 觸發條件：推送版本標籤 (例如: v1.0.0, v2.1.3)
name: Release - 自動發布

on:
  push:
    tags:
      - 'v*.*.*'  # 符合語義化版本標籤

# 設定工作流程權限
permissions:
  contents: write  # 允許創建 Release 並上傳檔案

jobs:
  build:
    name: 構建可執行檔 (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    
    # 多平台構建矩陣
    strategy:
      matrix:
        include:
          - os: windows-latest
            artifact_name: md2docx-windows.exe
            pyinstaller_opts: --windowed --icon=icon.ico
          - os: macos-latest
            artifact_name: md2docx-macos
            pyinstaller_opts: --windowed
          - os: ubuntu-latest
            artifact_name: md2docx-linux
            pyinstaller_opts: --onefile
    
    steps:
      # 步驟 1: 檢出程式碼
      - name: 📥 檢出程式碼
        uses: actions/checkout@v4
      
      # 步驟 2: 設定 Python 環境
      - name: 🐍 設定 Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      # 步驟 3: 安裝依賴與打包工具
      - name: 📦 安裝依賴
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
      
      # 步驟 4: 使用 PyInstaller 打包
      - name: 🔨 構建可執行檔
        run: |
          pyinstaller --name md2docx \
            --onefile \
            ${{ matrix.pyinstaller_opts }} \
            --add-data "README.md:." \
            md_to_word_converter.py
        shell: bash
      
      # 步驟 5: 重命名輸出檔案
      - name: 📝 重命名構建產物
        run: |
          if [ "${{ matrix.os }}" == "windows-latest" ]; then
            mv dist/md2docx.exe dist/${{ matrix.artifact_name }}
          else
            mv dist/md2docx dist/${{ matrix.artifact_name }}
            chmod +x dist/${{ matrix.artifact_name }}
          fi
        shell: bash
      
      # 步驟 6: 上傳構建產物
      - name: 📤 上傳構建產物
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/${{ matrix.artifact_name }}
          retention-days: 7

  release:
    name: 建立 GitHub Release
    needs: build  # 等待所有平台構建完成
    runs-on: ubuntu-latest
    
    steps:
      # 步驟 1: 檢出程式碼（獲取 CHANGELOG 等）
      - name: 📥 檢出程式碼
        uses: actions/checkout@v4
      
      # 步驟 2: 下載所有構建產物
      - name: 📥 下載構建產物
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      
      # 步驟 3: 建立 Release 說明
      - name: 📝 生成 Release 說明
        id: release_notes
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          
          # 生成 Release 說明
          cat > release_notes.md << EOF
          # Markdown to Word Converter $VERSION
          
          ## 📦 下載
          
          選擇您的作業系統下載對應的執行檔：
          
          - **Windows**: md2docx-windows.exe
          - **macOS**: md2docx-macos
          - **Linux**: md2docx-linux
          
          ## 🚀 使用方法
          
          1. 下載對應平台的執行檔
          2. (macOS/Linux) 賦予執行權限: \`chmod +x md2docx-*\`
          3. 雙擊執行檔啟動程式
          4. 選擇 Markdown 檔案並點擊「開始轉換」
          
          ## ✨ 功能特點
          
          - 將 Markdown 完美轉換為 Word 文件
          - 支援標題、列表、表格、程式碼區塊等
          - 使用 Word 內建樣式，無自定義樣式
          - 圖形化操作介面，簡單易用
          
          ## 📋 系統需求
          
          - Windows 10/11, macOS 10.14+, 或 Linux
          - 無需安裝 Python 或其他依賴
          
          完整說明請參閱 [README.md](https://github.com/${{ github.repository }}/blob/main/README.md)
          EOF
      
      # 步驟 4: 建立 GitHub Release
      - name: 🎉 建立 Release
        uses: softprops/action-gh-release@v1
        with:
          name: Release ${{ steps.release_notes.outputs.version }}
          body_path: release_notes.md
          draft: false
          prerelease: false
          files: |
            artifacts/md2docx-windows.exe/md2docx-windows.exe
            artifacts/md2docx-macos/md2docx-macos
            artifacts/md2docx-linux/md2docx-linux
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      # 步驟 5: 通知完成
      - name: ✅ 發布完成
        run: |
          echo "🎉 Release ${{ steps.release_notes.outputs.version }} 已成功發布！"
          echo "📦 下載地址: https://github.com/${{ github.repository }}/releases/tag/${{ steps.release_notes.outputs.version }}"
