# 持續整合工作流程
# 觸發條件：任何分支的 push 或 pull request
name: CI - 持續整合

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  test:
    name: 測試與代碼品質檢查
    runs-on: ${{ matrix.os }}
    
    # 多環境測試矩陣
    strategy:
      fail-fast: false  # 即使某個環境失敗，其他環境繼續測試
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ['3.8', '3.9', '3.10', '3.11']
    
    steps:
      # 步驟 1: 檢出程式碼
      - name: 📥 檢出程式碼 (Checkout)
        uses: actions/checkout@v4
      
      # 步驟 2: 設定 Python 環境
      - name: 🐍 設定 Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
          cache-dependency-path: |
            requirements.txt
            requirements-dev.txt
      
      # 步驟 2.5: 安裝 GTK3 依賴（僅 Linux）
      - name: 📦 安裝 GTK3 依賴 (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libpango-1.0-0 \
            libpangoft2-1.0-0 \
            libgdk-pixbuf2.0-0 \
            libffi-dev \
            shared-mime-info
      
      # 步驟 3: 升級 pip 與安裝依賴
      - name: 📦 安裝依賴套件
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
        continue-on-error: ${{ matrix.os == 'windows-latest' }}  # Windows 可能缺少 GTK3
      
      # 步驟 3.5: Windows 特殊處理（PDF 功能可選）
      - name: 📦 安裝基本依賴 (Windows - Word only)
        if: matrix.os == 'windows-latest'
        run: |
          pip install customtkinter python-docx packaging markdown
          pip install -r requirements-dev.txt
      
      # 步驟 4: 代碼格式檢查
      - name: 🎨 檢查代碼格式 (Black)
        run: |
          black --check --diff .
        continue-on-error: true
      
      # 步驟 5: 語法與風格檢查
      - name: 🔍 語法檢查 (Flake8)
        run: |
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=100 --statistics
      
      # 步驟 6: 執行單元測試 (Word 功能)
      - name: 🧪 執行 Word 轉換測試
        run: |
          pytest test_converter.py -v
        continue-on-error: false
      
      # 步驟 6.5: 執行 PDF 測試（僅 Linux/macOS）
      - name: 🧪 執行 PDF 轉換測試
        if: matrix.os != 'windows-latest'
        run: |
          pytest test_pdf_converter.py -v || echo "PDF test skipped (GTK3 may not be available)"
        continue-on-error: true
      
      # 步驟 7: 上傳測試覆蓋率報告
      - name: 📊 上傳覆蓋率報告
        uses: codecov/codecov-action@v4
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.11'
        with:
          file: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
      
      # 步驟 8: 功能驗證
      - name: ✅ Word 轉換功能驗證
        run: |
          python -c "from md_to_word_converter import MarkdownToWordConverter; print('Word converter OK')"
      
      - name: ✅ PDF 轉換功能驗證
        if: matrix.os != 'windows-latest'
        run: |
          python -c "from md_to_word_converter import PDF_AVAILABLE; print(f'PDF Available: {PDF_AVAILABLE}')"
        continue-on-error: true

  lint-security:
    name: 安全性與依賴檢查
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 檢out程式碼
        uses: actions/checkout@v4
      
      - name: 🐍 設定 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 安裝 GTK3
        run: |
          sudo apt-get update
          sudo apt-get install -y libpango-1.0-0 libpangoft2-1.0-0 libgdk-pixbuf2.0-0
      
      - name: 📦 安裝依賴
        run: |
          pip install -r requirements.txt
          pip install safety bandit
      
      - name: 🔒 安全漏洞掃描 (Safety)
        run: |
          safety check --json || true
        continue-on-error: true
      
      - name: 🛡️ 代碼安全掃描 (Bandit)
        run: |
          bandit -r . -f json -o bandit-report.json || true
        continue-on-error: true
